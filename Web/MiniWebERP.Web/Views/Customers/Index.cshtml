@model MiniWebERP.Web.ViewModels.Customers.CustomersListViewModel

@{
    ViewData["Title"] = "Customers";
}

    <section class="tables">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header d-flex align-items-center">
                            <a asp-controller="Customers" asp-action="Create" class="btn btn-outline-primary">Create New</a>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th>Company Name</th>
                                            <th>EIK</th>
                                            <th>City</th>
                                            <th>Address Line1</th>
                                            <th>Address Line2</th>
                                            <th>Contact Person</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var customer in Model.Customers)
                                        {
                                            <tr>
                                                <td>@customer.CompanyName</td>
                                                <td>@customer.EIK</td>
                                                <td>@customer.City</td>
                                                <td>@customer.AddressLine1</td>
                                                <td>@customer.AddressLine2</td>
                                                <td>@customer.ContactPerson</td>
                                                <td>
                                                    <a asp-action="Edit" asp-route-id="@customer.Id">Edit</a> |
                                                    <a asp-action="Details" asp-route-id="@customer.Id">Details</a> |
                                                    <a asp-action="Delete" asp-route-id="@customer.Id" class="text-danger">Delete</a>

                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
